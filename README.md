# 💻 Community Space - Frontend Web

Web client for Community Space service, built with React and Axios.

## 🛠 Tech Stack
- **Library:** React 18
- **State Management:** Context API (or Redux Toolkit)
- **Styling:** CSS Modules / Styled-components
- **API Client:** Axios
- **Routing:** React Router DOM

## 🌟 Key Features
- **Dynamic Feed:** Real-time post rendering and infinite scroll-like list management.
- **Image Handling:** User-specific home decorations and post image uploads with preview.
- **Integrated Auth:** Secure client-side routing and token-based authentication flow (JWT).

## ⚙️ How to Run
1. Ensure the Backend API is running at `http://localhost:8080`.
2. Clone the repository and install dependencies: `npm install`
4. Start the development server: `npm start`
5. Open [http://localhost:3000](http://localhost:3000) to view it in the browser.
